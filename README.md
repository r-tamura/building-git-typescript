# Building Git

An implementation of jit ([Building Git](https://shop.jcoglan.com/building-git/)) in TypeScript

## Development

```sh
# build
yarn watch

# test
yarn test

# run
# ./bin/jit <subcommand>
./bin/jit init
./bin/jit commit
./bin/jit add
```
